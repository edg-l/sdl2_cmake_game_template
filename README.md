# sdl2_cmake_game_template

## If using visual studio

CMake -> Change CMake Settings -> Project

Add to each configuration:

```json
"environments": [
        {
          "SDL2DIR": "C:\\SDL2-2.0.8"
        }
     ]
```
